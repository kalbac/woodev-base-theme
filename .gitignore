# Dependencies
node_modules/
vendor/

# Build output (release artifact only, not tracked)
woodev-base-theme/assets/dist/

# Env / local
.env
.env.*
*.local
.wp-env.override.json

# Tooling caches
.vite/
.phpunit.result.cache
.phpcs-cache
jscpd-report/
playwright-report/
test-results/

# Local-only handoff prompt
next-session-promt.md

# OS noise
Thumbs.db
.DS_Store

# Generated build intermediate (regenerate with `npm run tokens`)
src/css/tokens.generated.css
src/css/packs/
tests/integration/vendor/

# Serena MCP writes its per-project cache here; it is tooling state, not the project.
.serena/
